{
  "source": "index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "type": "module",
  "scripts": {
    "start": "parcel",
    "prebuild": "shx rm -rf dist/*",
    "build": "parcel build --public-url ./",
    "push-gh-pages": "push-dir --dir=dist --brach=gh-pages --local-branch-name=gh-pages --cleanup --verbose"
  },
  "devDependencies": {
    "@types/stats.js": "^0.17.0",
    "parcel": "latest",
    "push-dir": "^0.4.1",
    "sharp": "^0.31.3",
    "shx": "^0.3.4"
  },
  "dependencies": {
    "stats.js": "^0.17.0",
    "twgl.js": "^5.3.1"
  }
}
